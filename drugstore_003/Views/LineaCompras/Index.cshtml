@model IEnumerable<drugstore_003.Models.LineaCompras>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subtotal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precioCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Compras.idCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Productoes.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subtotal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precioCompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Compras.idCompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Productoes.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idLineaCompra }) |
            @Html.ActionLink("Details", "Details", new { id=item.idLineaCompra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idLineaCompra })
        </td>
    </tr>
}

</table>
