@model IEnumerable<drugstore_003.Models.DetalleLiquidacions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.monto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Conceptoes.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Liquidacions.idLiquidacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.monto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Conceptoes.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Liquidacions.idLiquidacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDetalle }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDetalle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDetalle })
        </td>
    </tr>
}

</table>
