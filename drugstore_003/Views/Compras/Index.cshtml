@model IEnumerable<drugstore_003.Models.Compras>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proveedors.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.usuario1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proveedors.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.usuario1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCompra }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCompra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCompra })
        </td>
    </tr>
}

</table>
